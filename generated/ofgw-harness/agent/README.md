# Agent Workspace

This directory is the generated markdown workspace for `ofgw-harness`.

Conventions:

- topic-specific artifacts go under `agent/<topic>/`
- shared markdown outputs can live directly under `agent/`
- manual-specific outputs do not belong here; use `generated/manual/`

This placeholder exists so the generated harness materializes the documented workspace policy instead of only describing it.
